# Artemis III candidate landing sites — PGDA Product 78
# Each entry uses the site catalog shorthand.
# Run `lunar_terrain_exporter batch --config config/artemis_sites.yaml --output-dir ./models` to regenerate.

sites:
  - site: connecting_ridge
  - site: de_gerlache_rim
  - site: peak_near_shackleton
  - site: shackleton_rim
